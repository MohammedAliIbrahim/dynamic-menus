@model dynamic_menus.Models.Menu

@{
    ViewBag.Title = "Delete";
}

    <div class="container">
        <h2 class="mb-5 mt-3">Delete</h2>

        <h3>Are you sure you want to delete this?</h3>
        <div>
           
            <hr />
            <dl class="dl-horizontal">
                <dt>
                  Parent Menu Name
                </dt>

                <dd>
                    @Html.DisplayFor(model => model.Parent.MenuName)
                </dd>

                <dt>
                    @Html.DisplayNameFor(model => model.MenuName)
                </dt>

                <dd>
                    @Html.DisplayFor(model => model.MenuName)
                </dd>

                <dt>
                    @Html.DisplayNameFor(model => model.MenuLink)
                </dt>

                <dd>
                    @Html.DisplayFor(model => model.MenuLink)
                </dd>

            </dl>

            @using (Html.BeginForm())
            {
                @Html.AntiForgeryToken()

                <div class="form-actions no-color">
                    <input type="submit" value="Delete" class="btn btn-default border border-primary  btn-outline-secondary" /> |
                    @Html.ActionLink("Back to List", "Index")
                </div>
            }
        </div>
        </div>